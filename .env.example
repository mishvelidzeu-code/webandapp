HOST=127.0.0.1
PORT=4173
PUBLIC_ORIGIN=http://127.0.0.1:4173
# Publishing is fail-safe: production is always published. Set SITE_DRAFT=true only for a local
# noindex build (the legacy SITE_PUBLISHED=false still works).
SITE_DRAFT=false
SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
LEAD_RECIPIENT=mishvelidze.u@gmail.com
# Set a strong random value on the server; never expose secrets in HTML.
CSRF_SECRET=
# One Node process: the included in-memory rate limiter is per instance.
# With a reverse proxy, keep Node bound to loopback. Do not trust forwarded IPs by default.
