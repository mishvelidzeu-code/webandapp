{
  "name": "webandapp-ge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/package-static.mjs && node scripts/verify-deploy.mjs",
    "dev": "node --env-file-if-exists=.env scripts/dev-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "audit": "node scripts/audit.mjs",
    "images": "node scripts/images.mjs",
    "verify:deploy": "node scripts/verify-deploy.mjs",
    "verify:live": "node scripts/verify-live.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "nodemailer": "^7.0.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "lighthouse": "^12.0.0",
    "playwright": "^1.50.0",
    "sharp": "^0.34.5"
  }
}
