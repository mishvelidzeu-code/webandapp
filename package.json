{
  "name": "webandapp-ge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/package-static.mjs",
    "start": "node --env-file-if-exists=.env server.mjs",
    "test": "node --test tests/*.test.mjs",
    "audit": "node scripts/audit.mjs",
    "verify:deploy": "node scripts/verify-deploy.mjs"
  },
  "engines": { "node": ">=22" },
  "dependencies": { "nodemailer": "^7.0.0" },
  "devDependencies": { "@axe-core/playwright": "^4.10.0", "lighthouse": "^12.0.0", "playwright": "^1.50.0" }
}
